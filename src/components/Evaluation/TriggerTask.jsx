import React, { useState, useEffect, useRef } from 'react';
import useWebSocket from 'react-use-websocket';

const TriggerTask = () => {
  return (
    <div>
      <h1>Device Change Notifications</h1>
    </div>
  );
};

export default TriggerTask;
